spring:
    datasource:
        url: jdbc:mysql://wangrui-flightdata.cj6gcogsidzt.ap-southeast-2.rds.amazonaws.com:3306/flight_db
        username: admin
        password: Qazxsw21.
        driver-class-name: com.mysql.cj.jdbc.Driver
        # url: jdbc:mysql://localhost:3306/flight-api
        # username: root
        # password: root
        # driver-class-name: com.mysql.cj.jdbc.Driver

        hikari:
            connection-timeout: 50000  # 连接超时时间，单位为毫秒，默认是30秒
            validation-timeout: 5000   # 连接验证超时时间，单位为毫秒
            idle-timeout: 600000       # 空闲连接超时时间，单位为毫秒，默认是10分钟
            max-lifetime: 1800000      # 连接的最大生命周期，单位为毫秒，默认是30分钟
        
        # url: ${SPRING_DATABASE_URL}
        # username: ${SPRING_DATABASE_USERNAME}
        # password: ${SPRING_DATABASE_PASSWORD}
        # url: jdbc:mysql://localhost:3306/flight-api

        # username: root
        # password: root
        # driver-class-name: com.mysql.cj.jdbc.Driver

    jpa:
        hibernate:
            # docker-compose.yml
            # ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO}
            ddl-auto: update
        show-sql: true

sql:
    init:
      mode: ${SPRING_SQL_INIT_MODE}
      
logging:
    level:
        org:
            hibernate:
                SQL: DEBUG

# JWT Configuration
jwt:
    secret-key: ThisIsAStrongSecretKey1234567890abcdefghijklmnopqrstuvwxyz
    # 24 hours in milliseconds
    expiration: 86400000